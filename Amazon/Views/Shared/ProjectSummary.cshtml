@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@model Book

@*this page is to keep track of each single book and is printed out to the main webpage*@

<div class="card card-outline-primary m-1 p-1">

    <div class="bg-light p-1">
        <h4>@Model.Title</h4>
        <span class="badge bg-info" style="float:right">Category: @Model.Category</span>
    </div>
    <ul style="list-style: none">

        <li>Author: @Model.Author</li>
        <li>Publisher: @Model.Publisher</li>
        <li>ISBN: @Model.Isbn</li>
        <li>ClassificationL @Model.Classification</li>
        <li>Page Count: @Model.PageCount</li>
        <li>Price: $@Model.Price</li>
    </ul>



    <form method="post" asp-page="/Donate">
        <input type="hidden" asp-for="@Model.BookId"/>
        <input type ="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
        <span class="card-text">
            <button type="submit" class="btn btn-success btn-sm float-end">Add to cart</button>
        </span>
    </form>
</div>